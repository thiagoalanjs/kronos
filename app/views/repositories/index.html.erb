<p id="notice"><%= notice %></p>

<h1>Repositories</h1>

  <%= render 'repositories/breadcrumb' %>
  <br />
  <br />
  <br />
<table>
  <thead>
    <tr>
      <th>Url</th>
      <th>Rules</th>
      <th>Project</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @repositories.each do |repository| %>
      <tr>
        <td><%= repository.url %></td>
        <td><%= repository.rules %></td>
        <td><%= repository.project_id %></td>
        <td><%= link_to 'Show', repository %></td>
        <td><%= link_to 'Edit', edit_repository_path(repository) %></td>
        <td><%= link_to 'Destroy', repository, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Repository', new_repository_path %>
